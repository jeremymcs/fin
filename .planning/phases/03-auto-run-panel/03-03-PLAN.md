---
phase: 03-auto-run-panel
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/agent/agent_loop.rs
  - src/tui/app.rs
autonomous: true
requirements: [AUTO-04]

must_haves:
  truths:
    - "ContextUsage event is emitted after each AgentEnd in agent_loop.rs with correct pct"
    - "AutoModeStart is emitted before auto loop spawn in app.rs"
    - "AutoModeEnd is emitted after auto loop completes/cancels in app.rs"
  artifacts:
    - path: "src/agent/agent_loop.rs"
      provides: "ContextUsage emit after AgentEnd"
      contains: "ContextUsage"
    - path: "src/tui/app.rs"
      provides: "AutoModeStart/End emit sites at loop spawn/completion"
      contains: "AutoModeStart"
  key_links:
    - from: "src/agent/agent_loop.rs"
      to: "src/io/agent_io.rs"
      via: "io.emit(AgentEvent::ContextUsage { pct })"
      pattern: "ContextUsage"
    - from: "src/tui/app.rs"
      to: "src/io/agent_io.rs"
      via: "event_tx.send(AgentEvent::AutoModeStart)"
      pattern: "AutoModeStart"
---

<objective>
Wire ContextUsage emission from agent_loop.rs and AutoModeStart/End emission from app.rs auto loop spawn sites.

Purpose: Delivers AUTO-04 (context % display) data pipeline and auto-mode gating signals. The render layer (P04) reads context_pct and is_auto from WorkflowState.
Output: ContextUsage emitted after every AgentEnd, AutoModeStart/End emitted at 3 loop spawn sites in app.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-auto-run-panel/03-CONTEXT.md
@.planning/phases/03-auto-run-panel/03-RESEARCH.md
@.planning/phases/03-auto-run-panel/03-01-SUMMARY.md

<interfaces>
<!-- From P01: AgentEvent variants -->
```rust
AutoModeStart,
AutoModeEnd,
ContextUsage { pct: u8 },
```

<!-- From P01: pure compute function in widgets.rs -->
```rust
pub fn compute_context_pct(input_tokens: u64, context_window: u64) -> u8;
```

<!-- agent_loop.rs AgentEnd emit (line 252-254) -->
```rust
io.emit(AgentEvent::AgentEnd {
    usage: state.cumulative_usage.clone(),
}).await?;
```

<!-- AgentState has model field with context_window -->
```rust
state.model.context_window  // u64, available at AgentEnd emit point
state.cumulative_usage.input_tokens  // u64
```

<!-- app.rs auto loop spawn sites (grep results) -->
// Line 1286: run_loop call (initial /auto command)
// Line 1591: run_loop call (resume workflow)
// Line 1881: run_loop call (stage commands with "auto" name)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit ContextUsage after AgentEnd in agent_loop.rs</name>
  <files>src/agent/agent_loop.rs</files>
  <read_first>
    - src/agent/agent_loop.rs lines 240-258 (AgentEnd emit point — this is where ContextUsage goes)
    - src/agent/agent_loop.rs lines 1-20 (imports — need to add compute_context_pct)
    - src/agent/state.rs lines 27, 45 (cumulative_usage field)
    - src/tui/widgets.rs (compute_context_pct signature from P01)
  </read_first>
  <action>
**Option A confirmed by RESEARCH.md**: Emit `ContextUsage` from inside `run_agent_loop` right after the `AgentEnd` emit (line 252-255).

After the existing `AgentEnd` emit block, add:

```rust
// Emit context window utilization (D-07, D-08).
// Uses cumulative input_tokens vs model context_window. Clamped at 100.
let pct = crate::tui::widgets::compute_context_pct(
    state.cumulative_usage.input_tokens,
    state.model.context_window,
);
io.emit(AgentEvent::ContextUsage { pct }).await?;
```

This requires NO signature changes to `run_agent_loop` or `run_unit`. The `state.model.context_window` and `state.cumulative_usage.input_tokens` are already available at this point.

Copyright header: already present — no changes needed.
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/fin && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/agent_loop.rs` contains `AgentEvent::ContextUsage { pct }`
    - `src/agent/agent_loop.rs` contains `compute_context_pct`
    - `src/agent/agent_loop.rs` contains `state.cumulative_usage.input_tokens`
    - `src/agent/agent_loop.rs` contains `state.model.context_window`
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>ContextUsage event emitted after every AgentEnd with correct pct computed from cumulative input tokens and model context window.</done>
</task>

<task type="auto">
  <name>Task 2: Emit AutoModeStart/End at loop spawn sites in app.rs</name>
  <files>src/tui/app.rs</files>
  <read_first>
    - src/tui/app.rs lines 1270-1310 (first auto loop spawn — /auto command)
    - src/tui/app.rs lines 1565-1630 (second auto loop spawn — resume workflow)
    - src/tui/app.rs lines 1870-1900 (third auto loop spawn — stage command "auto")
  </read_first>
  <action>
Per D-13: AutoModeStart/End are TUI-layer signals emitted from app.rs, NOT from inside auto_loop.rs. There are 3 sites where `run_loop` is called with `LoopMode::Auto`:

**Site 1 (~line 1286):** Initial `/auto` command path.
- Before the `run_loop` call, add: `let _ = event_tx.send(AgentEvent::AutoModeStart);`
- After the loop completes (after the `tracing::info!` that logs the result), add: `let _ = event_tx.send(AgentEvent::AutoModeEnd);`

**Site 2 (~line 1591):** Resume workflow path (blueprint resume).
- Before `let resume_fut = async {`, add: `let _ = event_tx.send(AgentEvent::AutoModeStart);`
- After the `tokio::select!` loop `break`s (after the loop block, before continuing), add: `let _ = event_tx.send(AgentEvent::AutoModeEnd);`

**Site 3 (~line 1881):** Stage command with `"auto"` name.
- This path only triggers when `stage_name == "auto"` (line 1879). Inside the `if stage_name == "auto"` block:
  - Before the `run_loop` call, add: `let _ = event_tx.send(AgentEvent::AutoModeStart);`
  - After the `tracing::info!` that logs the result, add: `let _ = event_tx.send(AgentEvent::AutoModeEnd);`

**IMPORTANT: Do NOT add `workflow_state.model_display` updates in this task.** P02 owns all model_display wiring (in the AutoModeStart and ModelChanged drain loop handlers). This task only emits the events at spawn sites — the drain loop handlers in P02 consume them.

Copyright header: already present.
  </action>
  <verify>
    <automated>cd /Users/jeremymcspadden/Github/fin && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/tui/app.rs` contains at least 3 occurrences of `AutoModeStart`
    - `src/tui/app.rs` contains at least 3 occurrences of `AutoModeEnd`
    - P03 does NOT write to the AutoModeStart match arm in the drain loop (P02 owns that)
    - `cargo build` exits 0
    - `cargo test` exits 0
  </acceptance_criteria>
  <done>AutoModeStart emitted before each auto loop spawn, AutoModeEnd after completion/cancellation. All 3 spawn sites covered. model_display wiring handled by P02 drain loop handlers.</done>
</task>

</tasks>

<verification>
- `cargo build` exits 0
- `cargo test` exits 0
- grep confirms ContextUsage emit in agent_loop.rs
- grep confirms AutoModeStart/End at all 3 spawn sites
</verification>

<success_criteria>
- ContextUsage emitted after every AgentEnd with correct pct from model.context_window
- AutoModeStart emitted before all 3 auto loop spawn sites
- AutoModeEnd emitted after all 3 auto loop completions
- No model_display writes in P03 (P02 owns that wiring)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-auto-run-panel/03-03-SUMMARY.md`
</output>
